{
  "scripts": {
    "dev": "rm -rf ./docs-dev ; parcel ./src/*.html --no-cache --no-source-maps --out-dir ./docs-dev --target browser",
    "build": "rm -rf ./docs ; parcel build ./src/*.html --no-cache --no-source-maps --out-dir ./docs --public-url /three --target browser"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "drag-drop": "^5.0.1",
    "parcel-bundler": "^1.12.4"
  },
  "devDependencies": {
    "sass": "^1.23.0"
  }
}
